# Core scientific stack
numpy>=1.26
pandas>=2.1
scipy>=1.11
pyarrow>=14.0

# Configuration / validation
pydantic>=2.5
pyyaml>=6.0

# Core ML
scikit-learn>=1.4

# Hyperparameter optimisation
optuna>=3.5

# Visualisation
matplotlib>=3.8
seaborn>=0.13

# Utilities
joblib>=1.3
tqdm>=4.66
rich>=13.7

# Optional components are intentionally not duplicated here. Install the
# complete platform with `pip install -e ".[all]"`, or select one of
# `classical`, `neural`, `serving`, and `explain`. For the complete development
# environment use `pip install -e ".[all,dev]"`.
